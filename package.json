{
  "name": "supatroppers",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently -n frontend,brain -c cyan,magenta \"pnpm -F frontend dev\" \"pnpm -F brain-server dev\"",
    "build": "pnpm -F frontend build && pnpm -F brain-server typecheck",
    "typecheck": "pnpm -r typecheck",
    "kill-server": "lsof -ti :8080 | xargs kill -9 2>/dev/null; echo done!",
    "prepare": "husky"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "husky": "^9.1.7"
  },
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=24"
  }
}
